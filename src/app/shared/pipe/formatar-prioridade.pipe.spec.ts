import { FormatarPrioridadePipe } from './formatar-prioridade.pipe';

describe('FormatarPrioridadePipe', () => {
  it('create an instance', () => {
    const pipe = new FormatarPrioridadePipe();
    expect(pipe).toBeTruthy();
  });
});
